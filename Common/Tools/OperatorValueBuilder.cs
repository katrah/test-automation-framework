using Common.Data;

namespace Common.Tools
{
    public class OperatorValueBuilder
    {
        /*public static OperatorValueBaseRequest New(string opStr, string valStr, bool useRegex)
        {
            if (useRegex)
            {
                return new OperatorRegexValueRequest(opStr, valStr);
            }
            return new OperatorValueRequest(opStr, valStr);
        }*/
    }
}
